/**
 * A course unit that carries marks (e.g. "CS101") — not the degree, see
 * types/program.js for that.
 *
 * @typedef {Object} Course
 * @property {number} id
 * @property {string} code
 * @property {string} name
 */

/**
 * @typedef {Object} CourseCreateInput
 * @property {string} code
 * @property {string} name
 */

export {};
